package ru.finney.pet.ui.theme

import androidx.compose.ui.graphics.Color

// Палитра из кадра айдентики в Figma. Значения менять только вместе с макетом:
// на этих шести цветах держится весь интерфейс, случайный оттенок сразу видно.

/** Обводка, текст и основной цвет. Обводка есть почти у всего — отсюда он самый частый. */
val FinneyInk = Color(0xFF382C92)

/** Заливка кнопок и акцентов. */
val FinneyPeach = Color(0xFFFA987C)

/** Ошибка. */
val FinneyPink = Color(0xFFF66A85)

/** Успех. */
val FinneyGreen = Color(0xFF86CC8E)

/** Предупреждение. */
val FinneyBlue = Color(0xFF67A6DC)

/** Деньги, награда, достижения. */
val FinneyYellow = Color(0xFFF7DC8B)

// Подложки. В макете панели лежат на кремовом, а не на белом: белый рядом
// с насыщенной обводкой выглядит резко.

/**
 * Фон экрана и панелей. Снят пипеткой с экспортов кадров 1:2 и 1:61
 * (design/exports/ui/) — в обоих это основная подложка.
 */
val FinneyCream = Color(0xFFFFEDCD)

/** Фон поля ввода и вдавленных областей — на тон светлее кремового. */
val FinneySand = Color(0xFFFCE7BE)

/**
 * Блик в левом верхнем углу кнопок. В макете он непрозрачно белый, а не
 * полупрозрачный: на жёлтой заливке прозрачный блик почти не виден.
 */
val FinneyGlare = Color(0xFFFFFFFF)

// Производные оттенки. Это не седьмой и восьмой цвета палитры — в палитре
// по-прежнему шесть. Просто в эталоне встречаются два оттенка, которые
// числами в кадре айдентики не названы, а нарисованы.

/**
 * Тёмное кольцо монеты. Снято с `design/reference/Figma export finney/Frame 24.png`:
 * у монеты слоёная структура, и внутреннее кольцо темнее основного зелёного.
 */
val FinneyGreenDark = Color(0xFF2B8B70)

/**
 * Приглушённый основной цвет: выполненный пункт списка условий в эталоне
 * зачёркнут и заметно бледнее соседнего невыполненного.
 */
val FinneyInkFaded = Color(0xFF8279BB)
