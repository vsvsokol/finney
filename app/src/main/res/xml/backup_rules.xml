<?xml version="1.0" encoding="utf-8"?>
<!--
   Резервная копия выключена (allowBackup="false" в манифесте), см. data_extraction_rules.xml.
   Файл оставлен на случай, если копию включат: тогда исключается всё.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
</full-backup-content>
